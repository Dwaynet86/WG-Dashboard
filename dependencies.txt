#use the apt repository
sudo apt update
sudo apt install -y qrencode python3-venv python3-dev build-essential libffi-dev
# optionally for system-wide: python3-pip
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt



# Edit with sudo visudo
www-data ALL=(ALL) NOPASSWD: /usr/bin/pivpn, /usr/bin/qrencode

# make this file executible
chmod +x run.sh

#run command
sudo uvicorn main:app --host 0.0.0.0 --port 8000